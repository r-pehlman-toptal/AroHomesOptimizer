"""
API package.

Exposes a FastAPI application with:
- Health check
- Query Service endpoints for analytics queries A–D.

Authentication (e.g. Cognito JWT) can be wired via middleware or dependencies.
"""

