"""
ETL layer for building gold and aggregate tables.

This package is responsible for:
- Executing the canonical SQL in `sql/gold` and `sql/agg`.
- Applying lightweight Python‑side validations.
- Exporting analysis‑ready feature tables for modeling and optimization.
"""

